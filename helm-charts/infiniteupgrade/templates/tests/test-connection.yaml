apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infiniteupgrade.fullname" . }}-test-connection"
  labels:
    {{- include "infiniteupgrade.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infiniteupgrade.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
