apiVersion: v1
kind: Service
metadata:
  name: {{ include "infiniteupgrade.fullname" . }}
  labels:
    {{- include "infiniteupgrade.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infiniteupgrade.selectorLabels" . | nindent 4 }}
