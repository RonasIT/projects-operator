{{- $root := . -}}
{{- range .Values.workers.items }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ regexReplaceAll "_" .name "-" }}-worker
  labels:
    {{- include "laravel.labels" $root | nindent 4 }}
spec:
  replicas: {{ .replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "laravel.labels" $root | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "laravel.labels" $root | nindent 8 }}
    spec:
      {{- with $root.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "laravel.serviceAccountName" $root }}
      securityContext:
        {{- toYaml $root.Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ regexReplaceAll "_" .name "-" }}-worker
        image: "{{ $root.Values.image.repository }}:{{ $root.Values.image.tag }}"
        imagePullPolicy: IfNotPresent
        command: 
          - sh
          - -c
          - {{ .cmd }}
        resources:
          {{ toYaml ( .resources | default $root.Values.workers.resources ) | indent 10 | trim }}
{{- end }}
